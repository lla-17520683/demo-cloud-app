server:
  port: ${PORT:8090}
#  servlet:
#    context-path: /a

spring:
  application:
    name: cloud-app

  mongodb:
    uri: ${MONGODB_URI:mongodb://localhost:27017}
    database: ${MONGODB_DATABASE:cloud_app_db}

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSSXXX} [%level] [%thread] %logger - %msg%n"
  level:
    root: ${LOG_LEVEL:INFO}

api:
#  number-service-url: ${NUMBER_URL:https://iden-service.yellowriver-8d419010.indonesiacentral.azurecontainerapps.io}
  number-service-url: ${NUMBER_URL:http://localhost:8080}
  net-url: ${NET_URL:http://dotnet-service:8085}